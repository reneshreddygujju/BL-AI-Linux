version = "1.0"

[metadata]
name = "Microservices Circular Dependency Resolution"
description = "Fix a broken microservices deployment with circular dependencies and configuration issues"
difficulty = "hard"
tags = ["devops", "kubernetes", "microservices", "debugging"]

[verifier]
timeout_sec = 600.0

[agent]
timeout_sec = 600.0

[environment]
build_timeout_sec = 600.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []

[environment.runtime]
python = "3.12"

[verifier.env]

[solution.env]
